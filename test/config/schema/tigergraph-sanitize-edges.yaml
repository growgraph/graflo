general:
    name: tigergraph-sanitize-edges
database_features:
    db_flavor: tigergraph
resources:
-   resource_name: parcels
    apply:
    -   vertex: parcel
    -   map:
            parcel_id: id
-   resource_name: boxes
    apply:
    -   vertex: box
    -   map:
            box_id: id
-   resource_name: containers
    apply:
    -   vertex: container
    -   map:
            container_name: name
vertex_config:
    vertices:
    -   name: parcel
        fields:
        -   name: id
            type: INT
        identity:
        -   id
    -   name: box
        fields:
        -   name: id
            type: INT
        identity:
        -   id
    -   name: container
        fields:
        -   name
        identity:
        -   name
edge_config:
    edges:
    -   source: box
        target: parcel
        relation: contains
    -   source: box
        target: box
        relation: contains
    -   source: container
        target: box
        relation: contains
    -   source: container
        target: container
        relation: package
    -   source: container
        target: container
        relation: box
