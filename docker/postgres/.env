# PostgreSQL Docker Configuration
# This file is used by PostgresConfig.from_docker_env()

# Docker Compose settings
IMAGE_VERSION=postgres:16-alpine
SPEC=graflo
CONTAINER_NAME="${SPEC}.postgres"

# Connection URI (full connection string)
# If not set, will be constructed from POSTGRES_HOSTNAME, POSTGRES_PORT, and POSTGRES_PROTOCOL
# POSTGRES_URI=postgresql://localhost:5432/postgres

# Connection components (used if POSTGRES_URI is not set)
POSTGRES_HOSTNAME=localhost
POSTGRES_PORT=5432
POSTGRES_PROTOCOL=postgresql

# Alternative: PostgreSQL often uses POSTGRES_HOST instead of POSTGRES_HOSTNAME
# POSTGRES_HOST=localhost

# Authentication
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres

# Docker Compose environment variables (used by docker-compose.yml)
# These match the standard PostgreSQL Docker image environment variables
# POSTGRES_USER is already set above
# POSTGRES_PASSWORD is already set above
# POSTGRES_DB is already set above
POSTGRES_HOST_AUTH_METHOD=scram-sha-256
